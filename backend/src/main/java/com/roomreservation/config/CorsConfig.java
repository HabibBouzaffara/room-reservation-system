package com.roomreservation.config;

import org.springframework.context.annotation.Configuration;

/**
 * CORS configuration is now handled in SecurityConfig.java
 * This class is kept for reference but is no longer needed
 */
@Configuration
public class CorsConfig {
    // CORS is configured in SecurityConfig.java via corsConfigurationSource() bean
}
